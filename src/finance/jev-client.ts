/**
 * Moved to src/jev/client.ts — Jev is now a platform-wide engine (classification,
 * heuristics, gated workflows in src/jev), not a finance-only facility.
 * This re-export keeps the finance module's public surface unchanged.
 */
export * from '../jev/client';

