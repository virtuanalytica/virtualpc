# T-004 — Update ingestion writer to canonical table

## Objective
Update the ingestion service to write new ohlcv rows into the canonical table.

## Context
- Deliberation constraint: writer cutover must complete before reader redirection.
- Depends on T-002.

## Owned Files
- `src/ingestion/ohlcv_writer.ts`
- `tests/ingestion/ohlcv_writer.test.ts`

## Out-of-Scope
- Reader code
- Backfill logic

## Required Behavior
1. Change writes from `ohlcv_legacy` to `ohlcv`.
2. Keep error handling and retry semantics.
3. Update tests to assert canonical table rows.

## Acceptance Criteria
- [ ] Writer inserts into canonical table.
- [ ] Tests pass.

## Verification Commands
```bash
npm test -- tests/ingestion/ohlcv_writer.test.ts
```

## Implementation Agent
codex

## Risk Flags
- schema_migration
