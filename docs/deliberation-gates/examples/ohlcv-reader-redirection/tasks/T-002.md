# T-002 — Backfill canonical ohlcv table

## Objective
Backfill the canonical `ohlcv` table from the legacy table.

## Context
- Deliberation constraint: backfill must be idempotent and verified with row-count parity.
- Depends on T-001.

## Owned Files
- `src/db/backfill/ohlcv_backfill.ts`
- `tests/db/ohlcv_backfill.test.ts`

## Out-of-Scope
- Schema changes
- Reader redirection

## Required Behavior
1. Read from legacy `ohlcv_legacy` table.
2. Insert into canonical `ohlcv` table.
3. Support resume/retry without duplicates.
4. Log progress and mismatches.

## Acceptance Criteria
- [ ] Backfill is idempotent.
- [ ] Row count matches legacy table within tolerance.

## Verification Commands
```bash
npm test -- tests/db/ohlcv_backfill.test.ts
```

## Implementation Agent
codex

## Risk Flags
- schema_migration
