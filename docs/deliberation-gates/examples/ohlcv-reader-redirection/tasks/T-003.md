# T-003 — Create compatibility view for old readers

## Objective
Create a compatibility view that exposes the legacy schema on top of the canonical table.

## Context
- Deliberation constraint: compatibility view must exist before any reader redirection.
- Depends on T-001.

## Owned Files
- `src/db/migrations/20260614_ohlcv_compat_view.sql`
- `src/db/schema.ts`

## Out-of-Scope
- Writer migration
- Reader redirection

## Required Behavior
1. Create view `ohlcv_legacy_view` that mirrors old column names and types.
2. Map canonical columns to legacy names.

## Acceptance Criteria
- [ ] View schema matches legacy table.
- [ ] SELECT parity test passes.

## Verification Commands
```bash
npm run db:migrate
npm test -- tests/db/compat_view.test.ts
```

## Implementation Agent
codex

## Risk Flags
- reader_redirection
