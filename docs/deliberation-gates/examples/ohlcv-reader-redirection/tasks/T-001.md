# T-001 — Create canonical ohlcv table

## Objective
Create the new canonical `ohlcv` table with the target schema.

## Context
- Deliberation constraint: canonical table must exist before backfill and compatibility view.
- No reader redirection in this task.

## Owned Files
- `src/db/migrations/20260614_create_canonical_ohlcv.sql`
- `src/db/schema.ts`

## Out-of-Scope
- Backfill logic
- Compatibility view
- Reader redirection
- Writer update

## Required Behavior
1. Define the canonical `ohlcv` table with columns: `symbol`, `timestamp`, `open`, `high`, `low`, `close`, `volume`, `source`.
2. Add primary key on `(symbol, timestamp, source)`.
3. Add indexes for common read patterns.

## Acceptance Criteria
- [ ] Migration runs successfully.
- [ ] Schema file is updated.

## Verification Commands
```bash
npm run db:migrate
npm test -- tests/db/schema.test.ts
```

## Implementation Agent
codex

## Risk Flags
- schema_migration
